# 2026-08-31 全面修复任务上下文恢复记录

## 读取范围

- 已读取 `C:\Users\chengliang\.codex\memories` 下全部 32 个非 Git 内部记忆文件，共 4,330 行、328,626 字节。
- `memory_summary.md` 由当前任务上下文直接注入；其余 `MEMORY.md`、`raw_memories.md`、26 份 rollout summary、ad-hoc 说明与记录、技能说明均已读取到文件末尾。
- 读取过程只恢复上下文，没有修改运行配置、用户数据或项目功能。

## 已恢复的项目决策

1. KeenCode 是本地优先的纯桌面应用；前端只维护可丢弃投影，Rust 端直接使用进程内 ACP 和供应商化 Agent 核心。
2. 模型协议映射保持唯一：`responses -> openai_responses`、`chat_completions -> openai`、`messages -> anthropic`；供应商热更新已通过稳定 `provider_id` 重建既有会话 Provider。
3. `usageReported=false` 表示用量未知，不能显示为真实零；reasoning token 是输出 token 的子项，不得重复计入总量。
4. 会话模型选择按 session 隔离；新会话草稿在首次成功发送前不持久化。
5. Windows 子进程复用现有无窗口启动封装；PTY 启动前规范化扩展路径；真实桌面交互验证不能用静态检查或可访问性树代替。
6. 子 Agent 完成结果目前不会自动唤醒已结束的父回合；若产品要求父任务等待子任务收敛，需要运行时 join 状态，不能只依赖提示词。
7. 所有提交只包含当前原子任务文件，并使用以中文为主的中英文双语提交信息；提交前后记录工作区、暂存区和差异状态。

## 预存风险与待复核项

- `src/storage.rs:98` 曾报告未使用变量 `parent`。
- 最近一次 Rust workspace 记录为 245 项通过、5 项失败：3 项 Windows `PermissionDenied (code 5)`，2 项下载大小限制错误文本断言不匹配；另有 unused import、unused variable、dead code 警告。
- 当时工具链缺少 Clippy，不能继承“Clippy 已通过”的结论。
- Windows 窗口控制异常被静默吞掉，滚动合成修复、PTY 路径修复和无窗口进程修复均存在真实桌面或当前工具链复验缺口。
- 顶部新建会话入口创建无项目草稿、项目行入口才绑定项目；历史记录只完成诊断，未确认当前产品语义是否需要调整。
- 前端构建历史上存在 chunk size 和 dynamic import 告警。
- 旧的“全功能桌面验收”主要证明控件可见，不足以证明所有用户流程可执行。

以上项目只作为本次全面审计的输入，不直接假定仍存在；阶段 0.2 和阶段 1 会在当前 `main` HEAD 上逐项复现、归因和更新状态。

## 本轮执行边界

- 不修改机密、认证、支付、数据库模式或部署配置。
- 不引入旧数据兼容层；项目公开发布前只维护当前唯一结构。
- 每个编号项独立完成审计、实现、验证和提交，再进入下一项。
- 每阶段结束后只新增小型 ad-hoc 记忆更新说明，不直接改写或删除已有记忆文件。
- 外部官方资料或 GitHub 直连失败时，按既有记忆要求通过 `http://127.0.0.1:7890` 重试后再判断不可用。
