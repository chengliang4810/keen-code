# KeenCode 项目说明

## 项目目标

打造一款**纯桌面端、开源、低内存、低资源占用、低分发体积**的 AI 编码工具。

产品应优先服务个人开发者的本地编码工作流：在一个轻量桌面应用中完成项目打开、代码理解、任务对话、文件修改、命令执行、变更审查和 Git 操作。

“纯桌面端”表示主应用、工作区状态和执行流程均运行在用户本机，不依赖必须部署的配套 Web 服务。应用可以按用户配置连接本地模型、远程模型 API 或外部编码代理，但不得把浏览器页面作为主要产品形态。

## 核心原则

1. **轻量优先**：内存、CPU、启动速度和安装包体积属于核心产品指标，不是后期优化项。
2. **本地优先**：项目文件、会话状态、扩展配置和工具执行记录默认保存在本机。
3. **边界透明**：加入项目即授予该目录访问范围；运行时不维护信任或审批状态，但文件写入、命令执行和网络访问必须可审查。
4. **后端解耦**：桌面外壳、会话模型和工具系统不得绑定单一模型厂商或单一代理协议。
5. **简单可维护**：优先采用少依赖、低复杂度、长期可维护的实现，不为理论扩展性提前堆叠抽象。
6. **开源可复现**：构建流程、依赖、协议和关键设计决策应公开、可审计、可重复验证。
7. **实测驱动选型**：技术栈通过源码分析、原型和基准测试逐步确定，不凭流行度或主观印象拍板。

## 初始产品范围

首个可用版本聚焦以下能力：

- 打开和管理本地代码项目。
- 在单个项目中创建多个独立对话，并允许不同对话并行运行。
- 与 AI 进行面向编码任务的多轮交互。
- 搜索、读取、编辑文件并展示可审查的差异。
- 执行终端命令，并展示可审查的执行过程与结果。
- 展示任务过程、工具调用、错误和最终结果。
- 提供基础 Git 状态、差异和提交辅助能力。
- 通过内置 Agent 运行时接入主流模型 API 与兼容接口，KeenCode 桌面层不实现模型协议适配。
- 支持单层子 Agent 创建、任务分派、状态跟踪、停止、重试和完成报告。
- 支持项目级持久 Goal、会话级 Todo 和单层子 Agent 进度。
- 支持会话级计划模式：开启后 Agent 只读调研并产出实施计划（深入调研委派内置 plan 子代理），实施由用户关闭模式后触发，不引入审批弹窗。只读子代理的方案/报告文件统一保存在 `~/.keencode` 数据目录下按项目划分的沙箱中，不写入用户项目目录。
- 提供插件市场、Skills 和 MCP 三个同级扩展管理入口。
- 模型设置只管理用户配置的自定义供应商，不提供任何模型厂商的官方账户、套餐、额度或官方 Key。

## 非目标

初期不把以下内容作为优先方向：

- Web 版、移动端或必须联网使用的 SaaS 控制台。
- 团队协作、组织管理、计费和企业管理功能。
- 默认内置大模型权重、浏览器内核或外部脚本运行时。
- 在核心工作流稳定前建设团队级私有扩展市场或复杂插件沙箱。
- 为追求功能数量而复制完整 IDE；优先与现有编辑器和开发工具协作。
- 严格只读且需要用户批准后才能执行的 Plan 模式。
- 外部消息渠道、递归 Deep Swarm、Topic Channel 和共享 KV Context。
- Ultracode、Dynamic Workflow、Swarm 任务图（多 Agent DAG 调度）、递归 Workflow、Deep Swarm 和其他多层 Workflow 编排。
- 本地 Embedding、Memory Sideagent、Ambient Memory、PDF 和 Bedrock。

## 初始性能预算

以下指标是技术选型和原型验证的首版验收基线，后续应在明确的基准设备、操作系统和测试场景中校准：

- 单平台实际安装包不得超过 `50 MB`，不默认打包浏览器内核、外部脚本运行时或模型权重。
- 内存以“软件启动并稳定后的空闲占用”和“一个活跃 AI 会话运行时的占用”作为首版基准。
- 首版不以多会话同时运行时的总内存作为技术栈否决条件，但必须记录并发会话的增量占用。
- 空闲 CPU 使用率目标低于 `1%`，无任务时不持续轮询。
- 首个可交互窗口冷启动目标不超过 `1.5 秒`。
- 后台进程、文件监听器和网络连接按需启动，并在任务结束后及时释放。

如果某项核心能力必须突破预算，应先提供实测数据、收益说明和替代方案，再调整指标。

## 技术栈演进规则

后续分析参考项目和完善技术栈时，应至少比较以下维度：

- 安装包、解压后应用和增量更新体积。
- 冷启动时间、空闲内存、任务峰值内存和空闲 CPU。
- Windows、macOS、Linux 的支持成本与平台一致性。
- UI 表现、无障碍、输入体验和大规模代码文本渲染能力。
- 进程隔离、权限控制、自动更新和密钥存储安全性。
- 与本地文件、终端、Git、PTY 和 AI 代理协议的集成复杂度。
- 构建速度、依赖数量、供应链风险和长期维护成本。
- 开源许可证与商业使用兼容性。

重要技术决策必须记录事实、假设、取舍、验证数据和结论。优先通过小型原型验证高风险环节，再确定最终技术栈。

## 界面基线

- 桌面工作台以仓库当前 `src/` 与 `public/` 为固定源码基线。
- 必须直接复用当前组件、DOM 结构、CSS、设计令牌和公共资源，不允许根据截图重新编写近似实现。
- 可见的界面交互控件必须优先使用 `src/components/ui/` 中的 shadcn/ui 组件。业务组件不得直接新增原生 `<button>`、`<input>`、`<textarea>`、`<select>`、`<dialog>` 等控件，也不得重复实现 shadcn/ui 已提供的组件；缺少组件时，先使用项目包管理器和 shadcn CLI 查询并阅读当前版本文档，再引入和组合。
- 原生元素只允许用于 shadcn/ui 组件自身的底层实现、浏览器能力要求的不可见控件（例如隐藏文件选择器），以及 `contenteditable`、媒体元素等 shadcn/ui 没有等价语义的底层宿主。使用例外时必须在代码中说明原因，不得为例外另建一套可见控件样式。
- 统一界面必须使用 shadcn/ui 的既有变体、尺寸和语义化设计令牌；业务 `className` 只负责必要布局和产品特定结构，不得覆盖组件的颜色、字体、边框、圆角及交互状态。
- 后端与代理协议能力的删减不得无意改变默认工作台的布局、尺寸、间距、字体、颜色和交互状态。
- 每次影响界面的修改都必须与固定基线使用相同视口截图，并进行像素差异检查。
- 品牌、文案或产品语义必须替换时，应保留原有元素盒模型和层级，并明确记录有意差异。
- 修改第三方源码时必须保留对应许可证和原作者版权声明。

## 架构边界

无论最终采用何种语言和桌面框架，都应保持以下边界：

- **桌面界面层**：负责交互和状态展示，不维护工具审批或项目信任状态，也不直接耦合具体模型协议。
- **应用核心层**：直接使用进程内 ACP 的 Session、请求和事件，不建立第二套会话协议。
- **系统能力层**：负责文件系统、PTY、进程、Git、密钥和操作系统集成。
- **Agent 运行时层**：进程内嵌入供应商化的 Agent 核心；模型协议由该运行时内部处理，来源、许可证与修改记录保存在 `vendor/` 及第三方声明中。

首版不在 KeenCode 桌面层实现模型厂商协议转换或通用 AI 适配层。Rust 桌面后端直接依赖供应商化的 Agent 核心，负责 ACP JSON-RPC 传输和会话生命周期；前端仅生成可丢弃的界面投影。

## 开发要求

- 修改前先检查现有实现和约定，采用解决根因的最小改动。
- 在用户明确说明产品已经公开发布前，KeenCode 始终按全新项目处理：只保留当前唯一配置与数据结构，不添加旧字段迁移、旧枚举别名、历史路径回退、废弃 API 包装或其他向后兼容代码。
- 工具执行没有权限模式或审批分支；加入项目即授予该目录访问范围，不保存显式信任状态，也不显示项目信任或工具执行二次确认。
- 新功能同时评估内存、CPU、体积、启动速度和后台活动影响。
- 不引入无法说明必要性的新依赖；优先使用标准库和现有能力。
- 性能结论必须附带测试环境、测试步骤和结果，不使用“轻量”“更快”等无数据描述。
- 关键模块应可独立测试，协议与系统边界应便于替换和模拟。
- 用户数据默认不上传；任何遥测、联网和数据共享能力必须明确说明并可关闭。
