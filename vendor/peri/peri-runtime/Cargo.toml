[package]
name = "peri-runtime"
version.workspace = true
edition.workspace = true
description = "Runtime layer — multi-session orchestrator: session_id -> SessionHandle map + event stamping"

[dependencies]
# 层依赖（§0 依赖规则）：仅 peri-acp-types（接口契约）/ peri-agent（Agent 层，
# 句柄实现与装配随 L5 落地）；不依赖 peri-acp / peri-tui / peri-controller。
peri-acp-types = { path = "../peri-acp-types" }
peri-agent = { path = "../peri-agent" }
anyhow.workspace = true
async-trait.workspace = true
parking_lot.workspace = true
thiserror.workspace = true

[dev-dependencies]
tokio.workspace = true
